package com.aipclm.system.pilot.model;

/**
 * Crew role within a multi-pilot cockpit session.
 * <p>CAPTAIN is the Pilot-in-Command (PIC), typically left seat.
 * FIRST_OFFICER is the co-pilot (SIC), typically right seat.</p>
 */
public enum CrewRole {
    CAPTAIN,
    FIRST_OFFICER
}
